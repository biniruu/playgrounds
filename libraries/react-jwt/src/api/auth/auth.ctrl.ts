const register = async (ctx: string) => {
  //
}

const login = async (ctx: string) => {
  //
}

const check = async (ctx: string) => {
  //
}

const logout = async (ctx: string) => {
  //
}

export { check, login, logout, register }
